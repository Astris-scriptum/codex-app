# Scoring

v2.4-alpha introduces `CandidateScorer`.

Current dimensions:

- grammar
- symbolism
- rarity
- semantic coherence

The model is deterministic and intentionally simple. It establishes a replaceable scoring interface before adding deeper linguistic and symbolic analysis.
