# Workspace Persistence

v2.5-alpha introduces a simple JSONL candidate repository.

This is not the final workspace database. It is an intermediate persistence layer that makes candidate history durable while keeping the data model transparent and easy to inspect.
