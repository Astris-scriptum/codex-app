# Codex Studio v0.2-alpha / Engine v2.6-alpha

Adds permutation deduplication, canonical ordering, advanced search controls, source-pool catalogue, zero-result UI reset, and a local macOS app-bundle builder.

Build with `./scripts/build_macos_app.command`. Output: `dist/Codex Studio.app`.
